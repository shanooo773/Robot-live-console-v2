# PRODUCTION ENVIRONMENT CONFIGURATION
# Copy this file to backend/.env and update the values for your VPS deployment

# Database Configuration
DATABASE_TYPE=mysql
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=robot_console
MYSQL_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
MYSQL_DATABASE=robot_console

# JWT Secret Key for Authentication
# Generate a strong random key: openssl rand -base64 32
SECRET_KEY=CHANGE_THIS_TO_A_STRONG_SECRET_KEY_32_CHARS_OR_MORE

# CORS Origins - Update with your actual domain and VPS IP
# For production with domain: https://yourdomain.com,https://www.yourdomain.com
# For VPS with IP: http://YOUR_VPS_IP,https://YOUR_VPS_IP
PRODUCTION_CORS_ORIGINS=http://172.104.207.139,https://yourdomain.com

# Server Configuration
HOST=0.0.0.0
PORT=8000

# VPS Configuration - Update with your VPS IP or domain
VPS_URL=http://172.104.207.139

# Video Storage Configuration
VIDEO_STORAGE_PATH=./videos
MAX_VIDEO_SIZE_MB=100

# Admin Configuration
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=CHANGE_THIS_ADMIN_PASSWORD

# Demo User Credentials (Optional - for testing)
DEMO_USER_EMAIL=demo@user.com
DEMO_USER_PASSWORD=demopassword
DEMO_ADMIN_EMAIL=admin@demo.com
DEMO_ADMIN_PASSWORD=demoadmin

# CRITICAL: Set to production for VPS deployment
ENVIRONMENT=production

# DEPLOYMENT NOTES:
# 1. Replace all placeholder values above with actual production values
# 2. Generate strong passwords and secret keys
# 3. Use HTTPS URLs in PRODUCTION_CORS_ORIGINS for SSL-enabled domains
# 4. Ensure ENVIRONMENT=production for production deployment
# 5. Keep this file secure (permissions 644, owned by service user)